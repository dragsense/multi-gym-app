export enum EAutomationTrigger {
    ONBOARD = 'onboard',
    BILLING = 'billing',
    CHECKIN = 'checkin',
    CHECKOUT = 'checkout',
    MEMBERSHIP_RENEWAL = 'membership_renewal',
}

export enum EAutomationFormat {
    EMAIL = 'email',
}

export enum EAutomationStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}
